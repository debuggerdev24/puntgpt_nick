import 'package:flutter/material.dart';

class PunterClubScreen extends StatelessWidget {
  const PunterClubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Punter Club Screen"));
  }
}
