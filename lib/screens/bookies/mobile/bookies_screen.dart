import 'package:flutter/material.dart';

class BookiesScreen extends StatelessWidget {
  const BookiesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bookies Screen"));
  }
}
